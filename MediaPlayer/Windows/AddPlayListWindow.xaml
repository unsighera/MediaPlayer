<Window x:Class="MediaPlayer.Windows.AddPlayListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MediaPlayer.Windows"
        mc:Ignorable="d"
        Title="Add Playlist" Height="300" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="PlaylistNameTextBox" Grid.Row="0" Margin="5" Text="Enter playlist name" GotFocus="PlaylistNameTextBox_GotFocus"/>
        <ListBox x:Name="MusicSelectionListBox" Grid.Row="1" Margin="5" DisplayMemberPath="FileName" SelectionMode="Multiple">

        </ListBox>
        <Button Content="Add Playlist" Grid.Row="2" Margin="5" Click="AddPlaylist_Click"/>
    </Grid>
</Window>